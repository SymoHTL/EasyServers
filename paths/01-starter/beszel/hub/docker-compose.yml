services:
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: unless-stopped
    ports:
      - "8090:8090" # web UI
    volumes:
      - beszel_data:/beszel_data

volumes:
  beszel_data:
